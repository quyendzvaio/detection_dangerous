version: '3.8'

services:
  triton-server:
    image: nvcr.io/nvidia/tritonserver:24.01-py3
    container_name: factory_triton_server
    ports:
      - "8000:8000" # http
      - "8001:8001" # grpc
      - "8002:8002" # matrics
    volumes:
      - ./triton_model_repo:/models
    command: [ "tritonserver", "--model-repository=/models" ]
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]
    shm_size: '2gb'
