# ==============================================================================
# RUNTIME DEPENDENCIES (main.py, tracker, Re-ID & PPE pipelines)
# ==============================================================================
ultralytics>=8.2.0          # YOLOv8-pose + BoT-SORT tracking (chạy nội bộ)
opencv-python>=4.9.0.80     # Đọc camera/video, cắt ảnh, hiển thị UI
numpy>=1.24.0               # Xử lý ma trận ảnh & vector đặc trưng
tritonclient[grpc]>=2.40.0  # Client gRPC gọi Triton (OSNet Re-ID + 4 model PPE)
psycopg2-binary>=2.9.9      # PostgreSQL driver cho SQLAlchemy
sqlalchemy>=2.0.0           # ORM Database Toolkit

# ==============================================================================
# EXPORT-ONLY DEPENDENCIES (export_scripts/ - dùng khi export .pt -> .onnx)
# Có thể bỏ qua ở môi trường chạy thật nếu model ONNX đã sẵn sàng trong triton_model_repo
# ==============================================================================
torch>=2.2.0                # export_osnet.py, export_yolo.py
torchvision>=0.17.0         # phụ thuộc của torch/torchreid
torchreid                   # build & load model OSNet để export ONNX
pillow>=10.2.0              # xử lý ảnh phụ trợ cho export
