# ==============================================================================
# RUNTIME DEPENDENCIES (camera_worker + 4 nhánh phân tích)
# ==============================================================================
opencv-python>=4.9.0.80     # Đọc camera USB (V4L2/MJPG), letterbox, vẽ overlay
numpy>=1.24.0               # Xử lý ma trận ảnh, decode pose, vector đặc trưng
tritonclient[grpc]>=2.40.0  # Client gRPC gọi Triton (yolo_pose + osnet + ppe + fall)
boxmot>=19.0.0              # BoT-SORT tracking (appearance-free) trên detections thô
psycopg2-binary>=2.9.9      # PostgreSQL driver cho SQLAlchemy (backend)
sqlalchemy>=2.0.0           # ORM Database Toolkit (backend)

# ==============================================================================
# EXPORT-ONLY DEPENDENCIES (export_scripts/ - dùng khi export .pt/.keras -> .onnx)
# Có thể bỏ qua ở môi trường chạy thật nếu model ONNX đã sẵn trong triton_model_repo
# ==============================================================================
ultralytics>=8.2.0          # export_pose.py: YOLO11n-pose .pt -> ONNX
torch>=2.2.0                # phụ thuộc ultralytics + export_osnet.py
torchvision>=0.17.0         # phụ thuộc của torch/torchreid
torchreid                   # build & load model OSNet để export ONNX
tensorflow-cpu>=2.16        # export_fall.py: load model Keras
tf2onnx>=1.16               # export_fall.py: Keras -> ONNX
onnx>=1.16                  # graph surgery (uint8 input, Erfc->Erf)
onnxruntime>=1.17           # verify parity ONNX vs bản gốc
pillow>=10.2.0              # xử lý ảnh phụ trợ cho export
